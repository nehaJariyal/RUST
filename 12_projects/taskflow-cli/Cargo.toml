[package]
name = "taskflow-cli"
version = "0.1.0"
edition = "2021"
description = "CLI task manager - CV portfolio project in Rust"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
